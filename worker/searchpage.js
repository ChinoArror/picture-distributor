export function renderClassSearchPage() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Search Classes · PhotoFinder</title>
  <script src="https://cdn.jsdelivr.net/npm/jszip@3.10.1/dist/jszip.min.js"></script>
  <style>
    html,body{max-width:100%;overflow-x:hidden}img{max-width:100%}.result,.photo,.topin,.shell{min-width:0;max-width:100%}@media(max-width:600px){.search{flex-wrap:wrap}.search input{flex:1 1 calc(100% - 56px);width:auto;min-width:0}.download{max-width:calc(100vw - 24px);overflow:auto}}
    :root{--paper:rgba(255,255,255,.84);--ink:#122033;--muted:#64748b;--line:#d9e0e7;--brand:#0f172a;--accent:#95f0ff}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at top left,rgba(255,255,255,.96),transparent 34%),radial-gradient(circle at top right,rgba(245,191,118,.38),transparent 22%),linear-gradient(135deg,#f8f3eb,#eadac7 58%,#e6d4c0);color:var(--ink);font-family:"Segoe UI","PingFang SC",sans-serif}.top{position:sticky;top:0;z-index:10;background:rgba(248,243,235,.82);border-bottom:1px solid rgba(255,255,255,.7);backdrop-filter:blur(22px)}.topin{width:min(1180px,calc(100% - 28px));margin:auto;padding:18px 0}.brand{color:var(--ink);font-size:26px;font-weight:850;letter-spacing:-.04em;text-decoration:none}.search{display:flex;gap:10px;margin-top:14px}.search input{flex:1;min-width:0;border:1px solid var(--line);border-radius:999px;padding:13px 18px;font-size:16px;background:rgba(255,255,255,.88);box-shadow:0 8px 24px rgba(15,23,42,.06);outline:0;transition:box-shadow .2s,border-color .2s}.search input:focus{border-color:#89dceb;box-shadow:0 14px 32px rgba(15,23,42,.11)}button,.button{border:0;border-radius:999px;padding:12px 18px;background:var(--brand);color:#fff;font:700 14px inherit;cursor:pointer;text-decoration:none;transition:transform .18s ease,box-shadow .18s ease}button:hover{transform:translateY(-1px)}.secondary{background:#fff;color:#344054;border:1px solid var(--line)}button:disabled{opacity:.5;cursor:not-allowed}.iconSearch{width:46px;padding:0;display:grid;place-items:center}.iconSearch svg{width:19px}.shell{width:min(1180px,calc(100% - 28px));margin:26px auto 110px}.summary{color:var(--muted);margin:0 0 18px}.result{background:var(--paper);border:1px solid rgba(255,255,255,.8);border-radius:30px;padding:22px;margin-bottom:18px;box-shadow:0 20px 70px rgba(54,39,15,.1);backdrop-filter:blur(22px);animation:reveal .42s cubic-bezier(.22,1,.36,1) both}.result:nth-child(2){animation-delay:.06s}.result:nth-child(3){animation-delay:.12s}@keyframes reveal{from{opacity:0;transform:translateY(16px)}to{opacity:1;transform:none}}.head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.head h2{margin:0 0 5px;font-size:23px;letter-spacing:-.03em}.meta{color:var(--muted);font-size:14px}.classSelect{display:flex;align-items:center;gap:8px;white-space:nowrap}.grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(150px,1fr));gap:14px;margin-top:17px}.photo{position:relative;border:1px solid var(--line);border-radius:20px;overflow:hidden;background:#fff;box-shadow:0 10px 25px rgba(15,23,42,.07);transition:transform .18s}.photo:hover{transform:translateY(-3px)}.photo img{width:100%;aspect-ratio:1/1;display:block;object-fit:cover}.photoName{padding:10px 12px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:13px}.pick{position:absolute;top:8px;right:8px;width:38px;height:38px;padding:0;display:grid;place-items:center;background:rgba(255,255,255,.9);color:#344054;border:1px solid rgba(255,255,255,.9);box-shadow:0 8px 20px rgba(15,23,42,.18);backdrop-filter:blur(12px)}.pick svg{width:20px}.pick.active{background:linear-gradient(135deg,#06b6d4,#38bdf8);color:#fff}.download{position:fixed;left:50%;bottom:20px;transform:translateX(-50%);display:flex;gap:10px;align-items:center;padding:10px;border-radius:999px;background:rgba(15,23,42,.95);box-shadow:0 18px 50px rgba(15,23,42,.3);color:#fff;backdrop-filter:blur(18px);animation:reveal .25s both}.empty{padding:50px 20px;text-align:center;background:var(--paper);border:1px dashed #cbd5e1;border-radius:30px}.hidden{display:none!important}mark{background:#fef08a;border-radius:4px;padding:0 2px}@media(max-width:600px){.head{display:block}.classSelect{margin-top:12px}.grid{grid-template-columns:repeat(2,1fr)}.download{width:calc(100% - 24px);border-radius:24px;flex-wrap:wrap;justify-content:center}.download button{flex:1}}
  </style>
</head>
<body>
  <header class="top"><div class="topin"><a class="brand" href="/">PhotoFinder</a><form class="search" action="/search" role="search"><input id="query" name="q" type="search" maxlength="80" placeholder="Search open classes" required autofocus /><button class="iconSearch" type="submit" aria-label="Search"><svg viewBox="0 0 24 24" fill="none"><circle cx="11" cy="11" r="6.5" stroke="currentColor" stroke-width="2"/><path d="M16 16l4 4" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg></button></form></div></header>
  <main class="shell"><p id="summary" class="summary">Searching...</p><div id="results"></div></main>
  <div id="downloadBar" class="download hidden"><span id="selectedText">0 selected</span><button id="directButton" class="secondary">Download separately</button><button id="downloadButton">Download ZIP</button><button id="clearButton" class="secondary">Clear</button></div>
  <script>
    const query = new URLSearchParams(location.search).get("q") || "";
    const selected = new Map();
    const $ = (id) => document.getElementById(id);
    $("query").value = query;
    const esc = (value) => String(value || "").replace(/[&<>"']/g,(char)=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[char]));
    const icon = (active) => active ? '<svg viewBox="0 0 24 24" fill="none"><path d="M6.5 12.5l3.4 3.4 7.8-8" stroke="currentColor" stroke-width="2.8" stroke-linecap="round" stroke-linejoin="round"/></svg>' : '<svg viewBox="0 0 24 24" fill="none"><circle cx="12" cy="12" r="7.5" stroke="currentColor" stroke-width="2"/></svg>';
    async function load() {
      if (!query.trim()) { $("summary").textContent = "Enter a class name to search."; return; }
      try {
        const response = await fetch("/api/class-search?q=" + encodeURIComponent(query));
        if (response.status === 401) { location.href = "/login"; return; }
        const data = await response.json();
        if (!response.ok) throw new Error(data.error || "Search failed");
        $("summary").textContent = data.classes.length + " open class" + (data.classes.length === 1 ? "" : "es") + " found for “" + query + "”";
        $("results").innerHTML = data.classes.length ? data.classes.map(renderClass).join("") : '<div class="empty"><h2>No matching open classes</h2><p class="meta">Try fewer words, check spelling, or use a broader class name.</p></div>';
      } catch (error) { $("summary").textContent = error.message || "Search failed"; }
    }
    function renderClass(item) {
      return '<section class="result" data-class="' + esc(item.id) + '"><div class="head"><div><h2>' + highlight(item.name, query) + '</h2><div class="meta">' + item.photos.length + ' downloadable photo' + (item.photos.length === 1 ? '' : 's') + '</div></div><label class="classSelect"><input type="checkbox" onchange="toggleClass(\\'' + item.id + '\\',this.checked)" /> Select entire class</label></div><div class="grid">' + item.photos.map((photo) => '<article class="photo"><a href="' + esc(photo.url) + '" target="_blank"><img src="' + esc(thumb(photo.url)) + '" alt="' + esc(photo.name) + '" loading="lazy" onerror="this.onerror=null;this.src=\\'' + esc(photo.url) + '\\'" /></a><div class="photoName" title="' + esc(photo.name) + '">' + esc(photo.name) + '</div><button class="pick" data-photo="' + esc(photo.id) + '" aria-pressed="false" onclick="togglePhoto(\\'' + photo.id + '\\',this)">' + icon(false) + '</button></article>').join("") + '</div></section>';
    }
    function highlight(name, term) {
      const safe = esc(name); const words = term.trim().split(/\s+/).filter(Boolean).sort((a,b)=>b.length-a.length);
      return words.reduce((value,word) => value.replace(new RegExp("(" + word.replace(/[.*+?^$(){}|[\\]\\\\]/g,"\\$&") + ")","ig"),"<mark>$1</mark>"), safe);
    }
    function thumb(url) { const absolute = url.startsWith("http") ? url : location.origin + url; return "/cdn-cgi/image/width=360,height=360,fit=cover,quality=72,format=auto/" + encodeURI(absolute); }
    window.togglePhoto = (id, button) => { const photo = findPhoto(id); if (!photo) return; selected.has(id) ? selected.delete(id) : selected.set(id,photo); updateButton(button,id); sync(); };
    window.toggleClass = (classId, checked) => { const item = window.searchClasses.find((entry)=>entry.id===classId); if (!item) return; item.photos.forEach((photo)=>checked ? selected.set(photo.id,photo) : selected.delete(photo.id)); document.querySelectorAll('[data-class="'+CSS.escape(classId)+'"] [data-photo]').forEach((button)=>updateButton(button,button.dataset.photo)); sync(); };
    function findPhoto(id) { for (const item of window.searchClasses || []) { const photo=item.photos.find((entry)=>entry.id===id); if(photo)return photo; } }
    function updateButton(button,id){const active=selected.has(id);button.classList.toggle("active",active);button.setAttribute("aria-pressed",String(active));button.innerHTML=icon(active)}
    function sync(){const count=selected.size;$("downloadBar").classList.toggle("hidden",!count);$("selectedText").textContent=count+" selected"}
    function cookieHistory(){const row=document.cookie.split("; ").find((item)=>item.startsWith("pd_class_searches="));if(!row)return[];try{return JSON.parse(decodeURIComponent(row.slice(row.indexOf("=")+1)))}catch{return[]}}
    async function rememberSearch(resultCount){if(!query.trim())return;try{const me=await fetch("/api/me").then((response)=>response.json());if(me.user&&(me.user.authUuid||me.user.role==="admin"))return}catch{}const old=cookieHistory().map((item)=>typeof item==="string"?{query:item,createdAt:new Date().toISOString(),resultCount:0}:item);const next=[{query,createdAt:new Date().toISOString(),resultCount:Number(resultCount||0)},...old].slice(0,30);document.cookie="pd_class_searches="+encodeURIComponent(JSON.stringify(next))+"; Max-Age=31536000; Path=/; SameSite=Lax"}
    $("clearButton").onclick=()=>{selected.clear();document.querySelectorAll("[data-photo]").forEach((button)=>updateButton(button,button.dataset.photo));document.querySelectorAll('.classSelect input').forEach((input)=>input.checked=false);sync()};
    $("directButton").onclick=()=>{[...selected.values()].forEach((photo,index)=>setTimeout(()=>{const anchor=document.createElement("a");anchor.href=photo.url;anchor.download=photo.name;anchor.click()},index*180))};
    $("downloadButton").onclick=async()=>{const items=[...selected.values()];$("downloadButton").disabled=true;const zip=new JSZip();try{for(let i=0;i<items.length;i++){ $("downloadButton").textContent="Preparing "+(i+1)+" / "+items.length;const response=await fetch(items[i].url);if(!response.ok)throw new Error("Download failed");zip.file(String(i+1).padStart(2,"0")+"-"+items[i].name.replace(/[\\/:*?"<>|]/g,"-"),await response.blob())}const blob=await zip.generateAsync({type:"blob",compression:"DEFLATE",compressionOptions:{level:4}});const href=URL.createObjectURL(blob);const a=document.createElement("a");a.href=href;a.download="photofinder-class-search.zip";a.click();setTimeout(()=>URL.revokeObjectURL(href),1000)}catch(error){alert(error.message)}finally{$("downloadButton").disabled=false;$("downloadButton").textContent="Download ZIP"}};
    fetch("/api/class-search?q="+encodeURIComponent(query)).then(async(response)=>{if(response.status===401){location.href="/login";return null}const data=await response.json();if(!response.ok)throw new Error(data.error||"Search failed");return data}).then((data)=>{if(!data)return;window.searchClasses=data.classes;const count=data.classes.reduce((sum,item)=>sum+item.photos.length,0);rememberSearch(count);$("summary").textContent=data.classes.length+" open class"+(data.classes.length===1?"":"es")+" found for “"+query+"”";$("results").innerHTML=data.classes.length?data.classes.map(renderClass).join(""):'<div class="empty"><h2>No matching open classes</h2><p class="meta">Try fewer words, check spelling, or use a broader class name.</p></div>'}).catch((error)=>$("summary").textContent=error.message);
  </script>
</body></html>`;
}
